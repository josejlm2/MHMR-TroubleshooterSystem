<h1>Status#edit</h1>
%h2 Edit Status
-#if able to figure out how to use UI helpers use movie_path(@movie)
= form_tag status_path(@status), :method => :put do
 = label :status, :name, 'Status Name'
 = text_field :status, :name
 = submit_tag 'save changes'
= link_to 'back to Status list', status_path
