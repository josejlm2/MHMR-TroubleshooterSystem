= content_for(:title, "Tickets")

-#Find me in app/views/tickets/index.html.erb

%span.details.table
.logs.pagination
 =will_paginate
%table#tickets.pure-table.pure-table-bordered
 %thead
  %th  View
  %th  Edit
  %th  Delete
  %th  Task
  %th  Status
  %th  Submitted
  %th  Subject
  %th  Description
  %th  Category
  %th  Location
  %th  Room
  %th  Requestor
  %th  Phone
  %th  Email
  %th  RU
  
 %tbody
 - @tickets.each do |ticket|
  %tr{:class => cycle('','pure-table-odd')}
   %td= "view button"
   %td= link_to 'Edit Info', edit_ticket_path(ticket), :method => :get
   %td= link_to 'Delete', ticket_path(ticket), :method => :delete
   %td= ticket.id
   %td= Status.find(ticket.status_id).name
   %td= ticket.id
   %td= ticket.subject
   %td= ticket.description
   %td= Category.find(ticket.ticket_category_id).name
   %td= Location.find(ticket.location_id).name 
   %td= ticket.room 
   %td= ticket.requestor
   %td= ticket.phone
   %td= ticket.email
   %td= ticket.ru
= link_to 'Create ticket', new_ticket_path
